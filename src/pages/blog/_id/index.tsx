import { Component, Vue } from 'nuxt-property-decorator'

import './index.scss'

@Component({
  name: 'blog-detail-page',
})
export default class BlogDetailPage extends Vue {
  protected render() {
    return <section>_id</section>
  }
}
